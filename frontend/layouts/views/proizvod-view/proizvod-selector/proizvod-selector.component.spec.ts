/*import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { ShemaFormComponent } from './shema-form.component';

describe('ShemaFormComponent', () => {
  let component: ShemaFormComponent;
  let fixture: ComponentFixture<ShemaFormComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ ShemaFormComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(ShemaFormComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});*/
